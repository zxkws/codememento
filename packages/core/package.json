{
  "name": "@codememento/core",
  "version": "0.1.0",
  "description": "Core library for CodeMemento durable repository memory.",
  "keywords": [
    "ai",
    "agents",
    "documentation",
    "docs-as-code",
    "repository"
  ],
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zxkws/codememento.git",
    "directory": "packages/core"
  },
  "homepage": "https://github.com/zxkws/codememento#readme",
  "bugs": {
    "url": "https://github.com/zxkws/codememento/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --clean",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "yaml": "^2.8.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
